## my-api

This API project uses Express.js, Prisma ORM, and Docker Compose to accept, return, and store data.

## Getting Started

First, run the development server:

```bash
docker compose up --build
```

Use Postman or Bash to run requests to the server.
